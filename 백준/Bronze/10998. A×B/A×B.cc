#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <time.h>

//int main(void)
//{
//	int n, m;
//	scanf("%d %d", &n, &m);
//
//	int* column = (int*)malloc(sizeof(int) * n);
//}

int main(void)
{
	int a, b;
	scanf("%d %d", &a, &b);
	printf("%d", a * b);
}